public enum EUIEventState
{
    Click,
    Hovered,
    Exit,
    Max
}

public enum ESoundType
{
    BGM,
    Effect,
    UI
}

public enum EstatType
{
    Hp,
    Damage,
    Defense,
    AttackSpeed,
    MoveSpeed,
    Max
}

public enum EState
{
    Idle,
    Move,
    Attack,
    Dead
}

public enum EUIstate
{
    Main,
    ChangeTileSelect,
    ChangeUnitAndRoom
}
